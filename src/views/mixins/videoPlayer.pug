mixin videoPlayer(video = {})
    .videoPlayer#jsVideoPlayer
        video.videoPlayer__desktop
            //- source(src=`/${video.src}`, type="video/mp4")
            //- source(src=`/${video.src}`)
            //- aws 연결 후, 경로 수정
            source(src=video.src, type="video/mp4")
            source(src=video.src)
            span 현재 사용하고 있는 브라우저는 비디오 태그를 지원하지 않습니다.
        video.videoPlayer__mobile(controls)
            source(src=video.src, type="video/mp4")
            source(src=video.src)
            span 현재 사용하고 있는 브라우저는 비디오 태그를 지원하지 않습니다.
        .videoPlayer__progress
            .progress__filled
        .videoPlayer__controls
            .videoPlayer__column
                span#jsPlayButton
                    i.fas.fa-play
            .videoPlayer__column
                span.volume__wrap#jsVolumeWrap
                    span.volume__btn#jsVolumeBtn
                        i.fas.fa-volume-up
                    input.videoPlayer__volume#jsVolume(type="range", min="0", max="1", value="0.5", step="0.1")
                span
                    span#currentTime 00:00:00
                    span /
                    span#totalTime 00:00:00
            .videoPlayer__column
                span#jsFullScreen
                    i.fas.fa-expand